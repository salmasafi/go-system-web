import 'package:flutter/material.dart';

class AppColors {
  // Primary red - main accent color
  static const primaryBlue = Color(0xFFA61111); // kept as primaryBlue for compatibility

  static const mediumBlue700 = Colors.red; // kept for compatibility

  // Dark red for prominent actions
  static const darkBlue = Color(0xFF7A0404); // kept as darkBlue for compatibility

  // Pink for Clear button and secondary destructive actions
  static const clearPink = Color(0xFFFF6B9D);

  // Beige/yellowish for Hold button
  static const holdBeige = Color(0xFFFFD54F);

  // Light gray background for cards and sections (replaces lightBlueBackground)
  static const lightBlueBackground = Color(0xFFF5F5F5);

  // White for main backgrounds and cards
  static const white = Color(0xFFFFFFFF);

  // Light gray for borders and subtle dividers
  static const lightGray = Color(0xFFE0E0E0);

  // Dark gray for text
  static const darkGray = Color(0xFF333333);

  // Medium gray for secondary text and icons (replaces linkBlue)
  static const linkBlue = Color(0xFF757575);

  // Normal gray for shadow
  static const shadowGray = Colors.grey;

  // Success green for positive indicators
  static const successGreen = Color(0xFF4CAF50);

  // Orange for warnings or highlights
  static const warningOrange = Color(0xFFFF9800);

  // Purple for categories or secondary accents
  static const categoryPurple = Color(0xFF9C27B0);

  static const Color red = Color(0xFFF44336);
  static const black = Colors.black;

  // Convenience aliases
  static const primaryRed = primaryBlue;
  static const darkRed = darkBlue;
  static const lightBackground = lightBlueBackground;
  static const mediumGray = linkBlue;
}
